# Local .terraform directories
.terraform/

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Exclude override files as they are usually local only
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform variable files (if they contain secrets)
*.tfvars
*.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lock file (optional, but often committed for provider version consistency)
# Uncomment the next line if you prefer not to commit the lock file
#.terraform.lock.hcl

# Plan files
*.plan
tfplan
